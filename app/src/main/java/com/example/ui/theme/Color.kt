package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Volcanic Anthracite Background of the theme
val MidnightBg = Color(0xFF0F0F0F)

// High-fidelity Frosted Glass Translucent surfaces
val DeepSurface = Color(0x0EFFFFFF)      // bg-white/5 opacity (slightly enhanced to 6% for contrast)
val ActiveCard = Color(0x1EFFFFFF)       // Selected/active frosted glass container highlight
val BorderGrey = Color(0x19FFFFFF)       // Glass border (white/10 opacity)
val SolidSurfaceBg = Color(0xFF15131C)    // Opaque background for sheets & dialogs to prevent text overlap

// Design Palette text colors
val GreyText = Color(0xFF938F99)
val CompliantWhite = Color(0xFFE6E1E5)

// Exquisite Accent Palette (repaired for Frosted Glass theme representation)
val NeonCyan = Color(0xFFD0BCFF)         // M3 Glass Purple / Highlight
val NeonAmber = Color(0xFFFFB4A9)        // Warm Rose / High priority alert
val NeonTeal = Color(0xFF82D8FF)         // Spearmint Ocean / Medium priority alert
val NeonMagenta = Color(0xFFEADDFF)      // Soft Lavender / Low priority alert
val PremiumGlassBg = Color(0xFF1C1B1F)   // Dark background contrast for chips
val PremiumGlassBorder = Color(0xFF49454F)// Border color for non-active items
val CoreOnPrimary = Color(0xFF381E72)    // Dark contrasting text on GlassPurple
